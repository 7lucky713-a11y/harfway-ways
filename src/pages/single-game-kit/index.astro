<!doctype html><html lang="ja"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>HARF-WAY SINGLE GAME KIT</title><style is:global>:root{--bg:#101310;--panel:#191e1a;--text:#e8eee7;--muted:#98a39a;--line:#344037;--accent:#a4bf8e}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font-family:-apple-system,BlinkMacSystemFont,"Hiragino Sans","Yu Gothic",Meiryo,sans-serif}a{color:inherit;text-decoration:none}.wrap{max-width:1120px;margin:auto;padding:28px}.hero{padding-top:52px;padding-bottom:32px}.hero small{color:var(--accent);font-weight:900}.hero h1{font-size:clamp(48px,8vw,90px);line-height:.88;margin:12px 0 20px;letter-spacing:-.06em}.hero p{max-width:760px;color:#b4beb5;line-height:1.8}.bar{display:flex;gap:10px;flex-wrap:wrap;margin:24px 0}.btn{border:1px solid var(--line);background:var(--panel);padding:11px 14px;border-radius:10px;font-weight:800}.btn.primary{background:var(--accent);border-color:var(--accent);color:#111}.grid{display:grid;grid-template-columns:1fr 1fr;gap:14px;padding-bottom:50px}.card{border:1px solid var(--line);background:var(--panel);border-radius:15px;padding:18px}.card h2{margin:7px 0;font-size:25px}.card p{color:var(--muted);font-size:13px;line-height:1.7}.slug{font:800 11px ui-monospace,monospace;color:var(--accent)}.links{display:flex;gap:8px;flex-wrap:wrap;margin-top:14px}.empty{grid-column:1/-1;border:1px dashed var(--line);border-radius:12px;padding:30px;color:var(--muted);text-align:center}@media(max-width:720px){.wrap{padding:20px}.grid{grid-template-columns:1fr}}</style></head><body><main class="wrap"><section class="hero"><small>HARF-WAY / DATA-DRIVEN SITE ENGINE</small><h1>SINGLE<br>GAME KIT</h1><p>ゲームサイト自体をコードではなくデータとして扱う。エンジンを一度公開した後は、Neonへサイト設定を追加するだけで新しい1ゲーム専門サイトを増やせます。</p><div class="bar"><a class="btn primary" href="/single-game-kit/sites-admin/">＋ゲームサイトを追加</a><span class="btn" id="storage">DB確認中…</span></div></section><section class="grid" id="sites"><div class="empty">サイト一覧を読み込んでいます…</div></section></main><script is:inline>(async()=>{const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),root=document.getElementById('sites'),storage=document.getElementById('storage');try{const r=await fetch('/api/single-game-sites',{cache:'no-store'}),d=await r.json();if(!r.ok||!d.ok)throw new Error(d.error||`http_${r.status}`);storage.textContent=`${d.environment==='production'?'Production Core':'Preview DB'} · ${d.sites.length} sites`;root.innerHTML=d.sites.length?d.sites.map(s=>`<article class="card"><span class="slug">/${esc(s.slug)}/</span><h2>${esc(s.siteName)}</h2><p>${esc(s.gameName)}<br>${esc(s.description)}</p><div class="links"><a class="btn primary" href="/single-game/${esc(s.slug)}/">公開ページ</a><a class="btn" href="/single-game/${esc(s.slug)}/admin/">記録管理</a></div></article>`).join(''):'<div class="empty">まだゲームサイトがありません。</div>'}catch(e){root.innerHTML=`<div class="empty">読み込みエラー：${esc(e.message||e)}</div>`;storage.textContent='DB接続エラー'}})();</script></body></html>
